import 'package:flutter/material.dart';
import 'package:http/http.dart' as http;

class MarkomHasilReportLead extends StatelessWidget {
  const MarkomHasilReportLead({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
